name: test

channels:
  - openeye
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - openeye
  - rdkit
  - numpy
  - networkx

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
    - nbval
