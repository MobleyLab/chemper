name: test

channels:
  - openeye
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - openeye-toolkits
  - rdkit
  - numpy
  - networkx

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
    - nbval
