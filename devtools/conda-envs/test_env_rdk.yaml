name: test

channels:
  - rdkit

dependencies:
    # Base depends
  - python
  - pip

  - rdkit
  - numpy
  - networkx

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
    - nbval
